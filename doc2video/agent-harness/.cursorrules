# Cursor Agent Rules — doc2video

## 项目说明
本项目是 CLI-Anything 生态中的 doc2video harness。
将文档（docx/md/pdf/txt）转换为带配音的 MP4 视频。

**渲染模式（二选一，默认用 Pillow）：**
| 模式 | CLI 参数 | 适用场景 |
|------|---------|---------|
| **Pillow**（推荐） | `--no-marp` | 高质量深紫背景，vlog-maker 同款风格 |
| **Marp** | 不加参数 | 需要 npm 环境，白底主题 |

> ⚠️ **默认使用 `--no-marp`**（Pillow 渲染器），无需 npm，无需 frontmatter CSS。

---

## 🎬 doc2video 完整工作流

当用户说"把这个文档做成视频" / "生成视频" / "转视频"时，**严格按以下步骤执行**，不要跳步：

### Step 1：读取并提炼文档（Pillow 模式）

阅读用户提供的文档，按以下规则生成 `/tmp/slides_<时间戳>.txt`：

> ⚠️ **Pillow 模式下不需要 Marp frontmatter，不要写 `---\nmarp: true` 开头**。
> 文件直接从第一页标题开始。

**内容规则：**
- 总页数：8-12 页（视频目标时长 6-8 分钟）
- 第一页：封面/引言（文档主题 + 一句话概述，无需 bullet）
- 中间页：每页聚焦一个核心概念，**5-8 条要点**
- 含「vs/对比/区别」的页：Pillow 自动触发左右两栏布局
- 最后一页：标题含「总结」或「回顾」，Pillow 自动触发结尾布局

**每页格式（严格遵守）：**
```
# 页面标题（一句话，≤ 20 字）

一句话副标题或背景说明（仅封面页第一页需要，其余页省略）

- 要点一（≤ 35 字，口语化，适合 TTS 朗读）
- 要点二（≤ 35 字）
- 要点三（≤ 35 字）
- 要点四（≤ 35 字）
- 要点五（≤ 35 字）

---
```

**写作要求：**
- 口语化表达，避免堆砌专业术语
- 每条要点像在"讲给人听"，不是"写给人看"
- 总文字量控制在 1200-1500 字（对应约 7-8 分钟 TTS）
- **每页至少 5 条要点**，不足的补充延伸说明或背景信息
- **不要写 HTML 注释**（`<!-- _class: lead -->`），Pillow 渲染器不支持

**完整示例：**
```
# 今天我们聊什么

深入解析 AI Agent 的设计模式与最佳实践

---

# 什么是 AI Agent

- AI Agent 是一种能自主完成任务的智能系统，不需要人工逐步干预
- 它的核心能力是：感知环境、做出决策、执行动作
- 和普通 AI 模型的区别在于：Agent 有记忆、有工具调用能力
- 典型应用：代码助手、自动化客服、数据分析 Agent
- 关键组成：LLM 大脑 + Memory 记忆 + Tools 工具 + Planning 规划

---

# 记忆系统对比

- 短期记忆 vs 长期记忆：前者是上下文窗口，后者是向量数据库
- 短期记忆的限制：token 窗口有限，对话一长就丢失早期信息
- 长期记忆的优势：持久化存储，跨会话保持用户偏好和历史
- 实现方案：RAG 检索增强 + Embedding 向量化
- 选型建议：短期任务用短期记忆，个性化产品必须上长期记忆

---

# 核心总结

- AI Agent = 感知 + 决策 + 执行，三要素缺一不可
- 记忆系统是区分「工具」和「助手」的关键能力
- Tool Calling 让 Agent 能真正操控外部世界
- 好的规划能力直接决定 Agent 完成复杂任务的成功率
- 从今天起，试着把你的工作流包装成一个 Agent
```

### Step 2：生成视频

```bash
cli-anything-doc2video process /tmp/slides_<时间戳>.txt \
  -o ./output_<时间戳>.mp4 \
  --no-marp \
  --voice zh-CN-XiaoxiaoNeural
```

**可用中文声音：**
| 声音 ID | 特点 |
|---------|------|
| `zh-CN-XiaoxiaoNeural` | 女声，温柔自然（**默认**）|
| `zh-CN-YunxiNeural` | 男声，沉稳清晰 |
| `zh-CN-XiaohanNeural` | 女声，干练 |
| `zh-CN-YunjianNeural` | 男声，有力 |

### Step 3：汇报结果

告知用户：
- 视频输出路径（绝对路径）
- 幻灯片页数
- 预计时长（页数 × 40 秒，估算值）
- 预览命令：`open output_<时间戳>.mp4`
- 渲染器：Pillow（深紫背景，vlog-maker 同款风格）

---

## 🔧 CLI 参考

```bash
# 基础用法（Pillow 高质量渲染，推荐）
cli-anything-doc2video process <文件> -o output.mp4 --no-marp

# 全参数
cli-anything-doc2video process <文件> \
  -o output.mp4 \
  --no-marp \
  --voice <声音ID> \
  --fps 24 \
  --no-tts       # 仅生成无声视频

# JSON 模式（Agent 解析结果用）
cli-anything-doc2video --json process <文件> -o output.mp4

# 单独调用子命令
cli-anything-doc2video parse <文件>          # 仅解析
cli-anything-doc2video tts <文件> -o out.mp3 # 仅生成语音
cli-anything-doc2video render <文件> --frames-dir /tmp/frames  # 仅渲染帧
```

**支持格式：** `.md` `.markdown` `.docx` `.doc` `.pdf` `.txt`

---

## ⚠️ 注意事项

1. **首次运行**需要安装依赖：
   ```bash
   cd doc2video/agent-harness && pip install -e .
   ```

2. **`--no-marp` 是默认推荐**：Pillow 渲染无需 npm，深紫背景，vlog-maker 同款视觉。

3. **TTS 需要网络**（调用微软 edge-tts 服务）。离线时加 `--no-tts`。

4. **输出路径用绝对路径**，避免找不到文件。

5. **文档太长时先提炼再生成**，不要原文照搬，每页控制在 5-8 条要点。

6. **slides 分页符**是单独一行的 `---`，第一页不需要在开头加 `---`。

7. **Pillow 模式不支持 HTML 注释**（如 `<!-- _class: lead -->`），写了会原样显示在画面上，必须删掉。

8. **布局自动识别规则**：
   - index=0（第一页）→ 封面布局（大标题居中）
   - title 含「vs/对比/区别」→ 左右两栏布局
   - title 含「总结/小结/回顾/结尾」→ 结尾布局（带进度条）
   - 其余页 → 标准 bullet list 布局
